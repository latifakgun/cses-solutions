#include <bits/stdc++.h>

using namespace std;

int main (){
    int q;
    cin >> q;

    while (q--){

        long long n, k;
        cin >> n >> k;

        if (k <= n / 2){
            cout << 2 * k << endl;
        }
        else {
            while (k > ((n+1)/2)){
                n = n - ((n+1)/2);
                k=
            }
        }


    }
}
